%h1.page-header ジョブ一覧

.container-fluid
  .row-fluid.pull-right
    .btn-toolbar{role: :toolbar}
      .btn-group
        =button_link('作成', url_for(controller: :jobs, action: :new), :glyph=>'plus')

=render partial: 'jobs/list', locals: {jobs: @jobs, videos: @videos}
=paginate @jobs
